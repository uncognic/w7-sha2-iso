# w7-sha2-iso
ISOs with the updates required for Windows 7 to recognize SHA2 signed drivers and updates.\
These are required on certain fresh installs of Windows 7, since they do not come with support for SHA2 signing.\
On these versions, SHA2, or lack thereof, prevents the installation of any updates or drivers that use this signing method.
